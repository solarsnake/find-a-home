# Core async & validation
playwright==1.44.0
playwright-stealth==1.0.6
pydantic==2.7.1
pydantic-settings==2.2.1
python-dotenv==1.0.1

# HTTP (for Redfin API calls without a full browser)
httpx==0.27.0

# CLI
click==8.1.7
rich==13.7.1

# Notifications
twilio==9.1.0
sendgrid==6.11.0

# Async file I/O
aiofiles==23.2.1

# Web API — future web/iOS backend (FastAPI serves the same engine as the CLI)
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Utilities
python-dateutil==2.9.0

# Realtor.com scraper (wraps Realtor.com's internal API — no browser needed)
homeharvest==0.3.4

# Data handling for HomeHarvest results
pandas>=2.0.0
